{
  "name": "safex-solutions-platform",
  "private": true,
  "version": "1.0.1",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix admin && npm install --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:admin": "npm run dev --prefix admin",
    "dev:server": "npm run dev --prefix server",
    "dev": "concurrently -n client,admin,server -c blue,magenta,green \"npm run dev:client\" \"npm run dev:admin\" \"npm run dev:server\"",
    "build": "npm run build --prefix client && npm run build --prefix admin"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
